# Runtime dependencies for the FastAPI backend (api:app).
#
# Deliberately excludes torch, transformers, prophet, arch, scikit-learn,
# matplotlib, plotly and streamlit: together they were ~814 MB against a
# 250 MB serverless bundle limit. Sentiment now runs through
# modules/finbert_backend.py, which uses FinBERT when it happens to be
# installed and Groq's hosted API otherwise.
#
# For local development with the local model, additionally:
#   pip install -r requirements-dev.txt

fastapi
pydantic>=2.0

yfinance>=0.2.66
pandas>=2.2.3
numpy>=1.26.4

requests>=2.32.3
beautifulsoup4>=4.12.3

cachetools>=6.0.0
